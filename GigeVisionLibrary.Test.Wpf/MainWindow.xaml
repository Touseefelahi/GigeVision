<Window x:Class="GigeVisionLibrary.Test.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GigeVisionLibrary.Test.Wpf"
        mc:Ignorable="d"
         xmlns:flatStyle="clr-namespace:FlatStyle;assembly=FlatStyle"
    Style="{StaticResource FlatWindow}"
    flatStyle:TitleBar.Value="30"
        Title="MainWindow" Height="450" Width="800">
    <DockPanel LastChildFill="True">
        <Button DockPanel.Dock="Top" Content="Start Stream" Click="Button_Click" HorizontalAlignment="Center" VerticalAlignment="Center" />

        <Grid>
            <Image x:Name="ImageControl" />
            <StackPanel VerticalAlignment="Top" HorizontalAlignment="Left">
                <local:LightImage2 x:Name="lightControl" />
            </StackPanel>
            <TextBlock x:Name="Fps" HorizontalAlignment="Right" VerticalAlignment="Bottom" Style="{StaticResource PrimaryBackgroundText}" />
        </Grid>
    </DockPanel>
</Window>